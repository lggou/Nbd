# 智能表单系统 - 项目规范

## 1. 概念与愿景

一个现代化的可视化表单构建平台，让用户能够通过直观的拖拽操作创建专业级表单。界面采用极简主义设计语言，以深色主题为基础，通过清晰的视觉层次和流畅的交互反馈，让复杂的表单设计变得轻松愉悦。

## 2. 设计语言

### 美学方向
采用 Linear 风格的深色极简界面：大量留白、微妙的玻璃态效果、精确的间距系统、克制的动画。

### 配色方案
- 主色：`#6366F1` (Indigo)
- 次色：`#8B5CF6` (Purple)
- 强调：`#22D3EE` (Cyan)
- 背景：`#09090B` (Zinc-950)
- 表面：`#18181B` (Zinc-900)
- 边框：`#27272A` (Zinc-800)
- 文本主：`#FAFAFA` (Zinc-50)
- 文本次：`#A1A1AA` (Zinc-400)

### 排版
- 主字体：`Inter`, system-ui, sans-serif
- 标题：18px/600, 14px/500
- 正文：14px/400
- 辅助：12px/400

### 空间系统
- 基础单位：4px
- 间距比例：4, 8, 12, 16, 24, 32, 48
- 圆角：6px (小), 8px (中), 12px (大)

### 动效哲学
- 所有过渡：150ms ease-out
- 拖拽反馈：即时，无延迟
- 模态框：200ms scale + opacity
- 列表项：staggered 50ms 延迟

## 3. 布局与结构

### 页面架构
- **主视图**：三栏式布局
  1. **左侧边栏** (240px)：字段组件库 + 表单列表管理
  2. **中央画布** (flex)：表单设计区域，实时预览
  3. **右侧属性面板** (320px)：选中字段的属性配置
- **独立预览页**：每个表单有独立的预览和填写界面

### 顶部工具栏
- 品牌logo
- 表单选择器（显示当前表单）
- 创建新表单按钮
- 表单设置按钮
- 预览/编辑模式切换
- 保存/导出按钮

### 左侧边栏结构
- **表单列表区**（可展开）
  - 显示所有已创建的表单
  - 新建表单按钮
  - 删除/重命名表单操作
- **字段组件库**
  - 按分组展示可用字段组件

### 响应策略
- Desktop (≥1280px)：完整三栏布局
- Tablet (768-1279px)：侧边栏可收起，双栏布局
- Mobile (<768px)：单栏，底部导航切换视图

## 4. 功能与交互

### 核心功能

#### 4.1 表单管理
- **创建表单**：点击新建按钮创建新表单
- **表单列表**：显示所有已创建的表单，支持切换
- **重命名**：双击表单名称可重命名
- **删除表单**：确认后删除表单
- **独立预览URL**：每个表单有独立的预览和填写链接

#### 4.2 字段组件库
可用字段类型：
- **输入类**：文本、数字、电话、邮箱、网址、密码
- **选择类**：下拉框、单选、多选、开关
- **日期时间**：日期、时间、日期时间范围
- **高级**：文件上传、图片上传、评分、滑动条
- **布局**：分隔线、段落文本、标题块

#### 4.3 拖拽交互
- 从组件库拖拽到画布：幽灵预览 + 插入指示线
- 画布内拖拽排序：实时位置预览
- 拖拽时显示蓝色插入线（2px）
- 松开后自动滚动到可视区域

#### 4.4 属性配置
选中字段后显示属性面板：
- **基础属性**：标签名、占位符、默认值、必填标记
- **验证规则**：最小/最大长度、正则表达式、自定义错误信息
- **样式选项**：宽度占比、显示条件
- **高级选项**：计算公式、联动逻辑

#### 4.5 表单预览
- 编辑模式内嵌预览
- 独立预览页面（支持直接分享链接）
- 实时验证显示
- 表单提交模拟
- 响应式布局测试

#### 4.6 用户认证
- **用户注册**：邮箱/密码注册
- **用户登录**：邮箱/密码登录
- **自动登录**：记住登录状态
- **用户信息**：查看和管理用户资料

#### 4.7 付款功能
- **付款字段**：添加支付金额字段
- **付款配置**：设置金额、货币、支付方式
- **支付流程**：选择支付方式、填写支付信息
- **支付模拟**：模拟支付成功/失败流程
- **支付记录**：查看支付历史和状态

#### 4.8 数据管理
- 自动保存到本地存储（已登录用户可同步到云端）
- 手动保存 JSON 配置
- 导入/导出表单配置
- 提交数据列表和统计

### 交互细节

#### 点击行为
- 单击字段：选中并打开属性面板
- 双击标签：直接编辑文本
- 单击空白处：取消选中

#### 键盘快捷键
- `Delete` / `Backspace`：删除选中字段
- `Ctrl/Cmd + S`：保存
- `Ctrl/Cmd + Z`：撤销
- `Ctrl/Cmd + D`：复制字段
- `↑/↓`：选择上/下一个字段
- `Escape`：取消选中/关闭面板

#### 空状态
画布为空时显示引导：
- "从左侧拖拽字段到此处开始"
- 常用字段快捷入口（文本、邮箱、数字）

#### 错误状态
- 验证失败：红色边框 + 抖动动画
- 保存失败：顶部 toast 通知

## 5. 组件清单

### 5.1 字段组件 (FieldComponent)
- 默认：深色背景、细边框
- 悬停：边框变亮 (Zinc-700)
- 选中：主色边框 + 蓝色阴影
- 拖拽中：半透明 + 放大 1.02

### 5.2 属性面板 (PropertyPanel)
- 垂直分组，带折叠功能
- 输入框：深色背景，聚焦时主色边框
- 开关：主色开启状态
- 下拉选择：自定义下拉样式

### 5.3 工具栏按钮 (ToolbarButton)
- 默认：透明背景，图标颜色 Zinc-400
- 悬停：Zinc-800 背景
- 激活：主色图标

### 5.4 模态框 (Modal)
- 背景遮罩：黑色 60% 透明度
- 容器：表面色，圆角 12px
- 进入动画：scale 0.95→1 + opacity

### 5.5 Toast 通知
- 成功：绿色左边框
- 错误：红色左边框
- 位置：右下角
- 自动消失：3秒

## 6. 技术方案

### 架构
- 单页应用，纯 HTML/CSS/JavaScript
- 无框架依赖，原生实现
- CSS 变量管理主题
- 模块化 JavaScript 结构

### 数据模型
```javascript
FormConfig = {
  id: string,
  name: string,
  fields: Field[],
  settings: FormSettings
}

Field = {
  id: string,
  type: FieldType,
  label: string,
  placeholder: string,
  defaultValue: any,
  required: boolean,
  validation: ValidationRules,
  options: Option[] // for select/radio/checkbox
}

FormSettings = {
  submitButtonText: string,
  successMessage: string,
  redirectUrl: string
}
```

### 存储策略
- LocalStorage 存储表单配置
- 提交数据独立存储
- 最大存储 50 个表单版本历史

### 浏览器支持
- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+
